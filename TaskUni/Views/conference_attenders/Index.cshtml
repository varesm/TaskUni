@model IEnumerable<TaskUni.conference_attenders>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.isAccepted)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Attender.firstname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.conference.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.isAccepted)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Attender.firstname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.conference.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
